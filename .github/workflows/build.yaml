name: Build Docker Image

on:
  pull_request:
    branches:
      - jazzy

jobs:
  build-ubuntu:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker Image
        run: |
          docker build -t fusion-node:latest .
